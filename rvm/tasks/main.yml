---
- name: install rvm
  sudo: no
  shell: curl -L https://get.rvm.io | bash -s stable --autolibs=enabled -- --ignore-dotfiles creates=/home/{{ansible_ssh_user}}/.rvm

- name: build {{version}}
  sudo: no
  shell: $HOME/.rvm/bin/rvm install {{version}}
  when: version is defined
